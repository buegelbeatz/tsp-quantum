---
layer: digital-generic-team
---
# Shared Skills Namespace

This directory is the canonical shared namespace.

During migration Phase 2, each skill under `shared/` delegates to the existing
legacy `shared-*` implementation to preserve backward compatibility.

Legacy top-level `shared-*` directories remain temporarily supported aliases
until caller migration is complete.
