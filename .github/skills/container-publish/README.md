---
layer: digital-generic-team
---

# container-publish

Governed GHCR publishing helper for delivery workflows.

The skill resolves repository-local container publishing metadata from `.digital-team/container-publish.yaml` and supports a paired documentation artifact so registry consumers can discover both the runnable image and the synced docs bundle.