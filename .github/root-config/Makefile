SHELL := /usr/bin/env bash
.DEFAULT_GOAL := help

# Optional: layer-specific targets (e.g. from a layer.mk in .digital-team/)
-include .digital-team/layer.mk

# All project make targets are centralized in .github/make/commands.mk.
include .github/make/commands.mk
