<!-- layer: tsp-quantum -->
# /project-board — Project Board

Alias for `/board --board project`. Shows the lifecycle board for the **Project** stage in terminal/chat output.

Default command:

```bash
make board BOARD=project
```

Source layer: `digital-generic-team`

See `/board` for board rendering, ref sync, and checkout workflow details.
